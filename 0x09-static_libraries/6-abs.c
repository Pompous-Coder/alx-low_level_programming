#include "main.h"
/**
 * _abs- Self explanatory function
 * @a:blah balahblah
 * Description: Printing all lower case alphabets.
 * Return: Always 0 if success
 */
int _abs(int a)
{
	if (a >= 0)
		return (a);
	else
		return (a * -1);
}
