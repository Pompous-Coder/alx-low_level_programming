#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main-Entry function
 * Return: Always 0 if success
 */
int main(void)
{
	int num_Val;

	for (num_Val = 0; num_Val <= 9; num_Val++)
		printf("%d", num_Val);
	putchar('\n');

	return (0);
}
